{
  "type": "object",
  "properties": {
    "estado": { "type": "string", "enum": ["NOVO", "VISTO", "EM SEGUIMENTO", "RESOLVIDO"] },
    "prioridade": { "type": "string", "enum": ["Baixa", "Normal", "Alta"] }
  },
  "anyOf": [
    { "required": ["estado"] },
    { "required": ["prioridade"] }
  ],
  "additionalProperties": false
}